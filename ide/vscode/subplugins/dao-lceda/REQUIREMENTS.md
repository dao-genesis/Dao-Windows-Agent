# vscode-dao-lceda · 需求全集(结构化) 与状态矩阵

> 正本清源: 把嘉立创EDA整体全方位路由进 VS Code 插件, 在 IDE 内操作一切本源与功能,
> 不再依赖 EDA 软件本体操作。汇总历次会话全部提示词需求, 先列完、明确完、结构完, 再大规模实践。

## R1 · EDA 整块路由(呈现面)

> 反者道之动: 本源不是"把画面截图再回灌坐标"(投屏), 而是**把 EDA 本体的真实页面
> 经本桥「中枢中转」直接搬进 IDE 面板** —— 真实 DOM、真实登录态、原生交互, 无一帧
> 截图、无一次坐标模拟。screencast 降级为显式兜底(`daoLceda.panelMode=screencast`)。

| # | 需求 | 状态 |
|---|---|---|
| R1.0 | **原生嵌入(本源·非投屏)**: `/native` 反代 EDA 真实页面进面板(剥离禁嵌头 X-Frame-Options/CSP + 注入 CDP 会话 cookie + 改写 <base>/绝对源/Location) | ✅ 本PR · 默认 |
| R1.1 | 帧流兜底: 中间面板承载 EDA 画面(CDP screencast → /api/frame) | ✅ 降级兜底 |
| R1.2 | 帧流兜底下鼠键输入回灌 EDA(/api/input) | ✅ 降级兜底 |
| R1.3 | Web 版(29229)/桌面版(29230) CDP 目标一视同仁自动发现 | ✅ |
| R1.4 | 桥自愈: 目标断线自动重连 | ✅ |

## R2 · 工程树(结构面)

| # | 需求 | 状态 |
|---|---|---|
| R2.1 | 左侧树显示当前工程/原理图页 | ✅ |
| R2.2 | 树节点可点击直调: 点原理图/PCB 即在 EDA 打开对应文档 | ✅ 本PR |
| R2.3 | 点其它工程 uuid 即切换工程 | ✅ 本PR |
| R2.4 | 作业完成后树自动刷新 | ✅ 本PR |

## R3 · 道之对话(Copilot/Augment 式 AI 交互层)

| # | 需求 | 状态 |
|---|---|---|
| R3.1 | 自然语言 → 工具编排 → 异步作业步骤流(Copilot 式) | ✅ |
| R3.2 | 会话历史持久化(Reload Window 后回放) + 清空会话 | ✅ |
| R3.3 | 上下文 chip 显示当前工程 | ✅ |
| R3.4 | `/` 斜杠命令目录 + 补全直调 | ✅ |
| R3.5 | 结果内联渲染(canvas.image → `<img>`) | ✅ (Blob→dataURL 修复已合入) |
| R3.6 | 快捷指令按钮行(工程信息/画布截图/DRC/全链路) | ✅ 本PR |

## R4 · 后端工具调用层(执行面 · 全链路)

| # | 需求 | 状态 |
|---|---|---|
| R4.1 | 全链路工具目录 /api/tools(建工程→放件→布线→同步→布局→板框→自动布线→覆铜→DRC→出产) | ✅ 14 工具 |
| R4.2 | 任意官方 EXTAPI 动词直调路由进插件(91 命名空间 · 操作一切本源) — `verb.call` | ✅ 本PR |
| R4.3 | 工程/文档级路由: `project.open` / `doc.open` / `sch.list` / `editor.version` | ✅ 本PR |
| R4.4 | /api/agent 支持显式多步 `plan`(外接更强 Agent 整体替换路由器) | ✅ 本PR |
| R4.5 | 未知工具名返回明确错误而非 500 | ✅ 本PR |

## R5 · IDE 级操控与周边

| # | 需求 | 状态 |
|---|---|---|
| R5.1 | 状态栏项一键打开面板 + 桥接健康态实时可见 | ✅ 本PR(健康轮询) |
| R5.2 | 独立命令「画布截图」直接开图像标签页 | ✅ 本PR |
| R5.3 | 冷启动底座: 一条命令登录+CDP 就绪(cold_start.py) | ✅ |
| R5.4 | 插件打包 vsix / serve-web 安装闭环实测 | ✅ 上会话已验证 |

## 遗留 / 后续

- 更强路由器: 接大模型替换规则 route()(经 R4.4 plan 通道即可外挂, 无需改桥)。
- 工程树下沉到器件/网络级(pcb_PrimitiveComponent 列表)。
- 作业取消/中断通道。
- 多工程并行会话隔离。
