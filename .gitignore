__pycache__/
*.pyc
.pytest_cache/
# 冷启动介质与镜像（大文件，不入库）
coldstart/windows-sim/media/
coldstart/windows-sim/images/
*.iso
*.qcow2
*.fd
node_modules/
# 账号注册表（含明文 RDP 口令，运行时生成，勿入库；样例见 accounts.example.json）
desktop/accounts.json

# 无头登录产出的 auth 束（含 bearer token，运行时生成，勿入库）
*.dao-auth.json
.dao/
devin_auth*.json
devin_login*.html

# 二合一构建装配产物(unify.js 折入的领域子模块, 构建时生成)
ide/vscode/vendor/

# 隧道会话租约运行时台账（窗口↔会话持久绑定）——运行时生成，不入库
desktop/sessions-state.json
desktop/sessions-state.json.tmp

# pip install -e . build metadata
*.egg-info/
